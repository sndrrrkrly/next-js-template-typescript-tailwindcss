export { NextPage } from 'next';

const Website: NextPage = () => {
     return (
          <>
               Next.js + TailwindCSS + TypeScript
               template starter
          </>
     );
};

export default Website;